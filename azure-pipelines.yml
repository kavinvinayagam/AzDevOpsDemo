trigger:
- master

pool:
  name: Default

steps:
- checkout: self

- script: dotnet restore **/*.csproj
  displayName: Restore

- script: dotnet build **/*.csproj --configuration Release
  displayName: Build